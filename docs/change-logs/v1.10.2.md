# v1.10.2 — 2026-04-29

## Fixes
- **aiwm/con**: agent response từ portal không còn bị forward sang Discord khi conversation ở `conversationMode = shared` — track `sourcePlatform` qua Redis key `conv:trigger-platform:{convId}` và skip outbound nếu trigger từ portal
