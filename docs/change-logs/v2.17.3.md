# v2.17.3 — 2026-06-12

## Fixes
- **aiwm/tool**: bỏ `required: true` khỏi `schema` field trong Mongoose schema — api-type tool không cần `schema` (inputSchema nằm per-function), nhưng Mongoose vẫn reject request dù DTO validation đã skip
