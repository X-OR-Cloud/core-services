# v2.4.3 — 2026-05-12

## Fixes
- **cbm/contact**: cho phép role `organization.editor` và `organization.viewer` đọc contacts (`GET /contacts`, `GET /contacts/:id`). Các thao tác ghi (create/update/delete/platform-links) vẫn owner-only.
