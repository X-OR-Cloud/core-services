# v2.1.0 — 2026-05-09

## Features

- **sys**: scaffold service mới (port 3007 dev, 3370–3379 prod) cho tập trung **runtime settings** + **centralized audit-log** — phục vụ toàn bộ core services qua lib `@hydrabyte/sys-client` (sẽ landed P1).
  - 2 module skeleton: `setting` và `audit-log` (chỉ schema, full impl ở P1/P2)
  - Multi-mode: `api` (port 3370–3371) + `wrk` (BullMQ audit-ingest, fork mode)
  - DB: `core_sys`
  - PM2 instances: `core.sys.api00`, `core.sys.api01`, `core.sys.worker00`

## Docs

- **proposal/plan/backlog**: thêm `docs/sys/PROPOSAL.md` (v2 — kiến trúc + 22 quyết định), `docs/sys/PLAN_v1.md` (7 phase implementation), `docs/sys/FEATURE_BACKLOG.md` (single source of truth tracking)

## Fixes

- **PORT-ALLOCATION.md** & **root CLAUDE.md**: cập nhật cho khớp thực tế
  - `schd` thực tế dùng 3006/3360–3369 (root CLAUDE.md trước đó ghi nhầm 3009/3390–3399)
  - `sys` mới: 3007/3370–3379
  - Bỏ entries của `pag`, `aivp`, `vsm` (đã removed ở v2.0.0)
  - "Next available ports": 3008+ (vì 3006 schd, 3007 sys)

## Notes

- P0 (scaffold) hoàn thành — `nx run sys:api` chạy được, `/health` và `/api-docs` trả 200. Build pass.
- P1 (module setting + lib `@hydrabyte/sys-client`) sẽ ở PR riêng tiếp theo (`feat/sys-setting`).
- Pilot service migrate sẽ là `iam` (P3), `aiwm.configuration` migrate cuối cùng (P5).
