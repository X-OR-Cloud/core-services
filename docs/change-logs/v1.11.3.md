# v1.11.3 — 2026-05-01

## Fixes
- **cbm/provider**: sửa `require('@payos/node')` thành `{ PayOS }` destructured — `@payos/node` export named chứ không phải default, gây lỗi `TypeError: PayOS is not a constructor`
- **cbm/payment**: áp dụng cùng fix cho `createPendingPayment()`
