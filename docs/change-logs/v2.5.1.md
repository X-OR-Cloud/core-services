# v2.5.1 — 2026-05-12

## Fixes
- **aiwm/config**: Thêm metadata cho `IAM_JWT_ACCESS_TTL_SEC` và `IAM_REFRESH_TOKEN_TTL_SEC` vào `CONFIG_METADATA` — fix lỗi build do thiếu entry cho 2 key mới trong `ConfigKey` enum.
