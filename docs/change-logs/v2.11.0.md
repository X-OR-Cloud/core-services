# v2.11.0 — 2026-05-29

## Features
- **aiwm/node**: thêm option `kubernetes` vào setup guide — sinh manifest `xnode-k8s.yaml` với setup token và URLs pre-filled; init container tự bootstrap lần đầu và persist credentials vào PVC, các lần restart sau bỏ qua bootstrap
- **aiwm/node**: đổi tên field `scriptContent` → `fileContent` trong setup guide response cho semantic rõ hơn (dùng chung cho cả `.sh` và `.yaml`)
