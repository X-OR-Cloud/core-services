# v2.11.2 — 2026-05-29

## Fixes
- **aiwm/chat-gateway**: sửa logic filter type khi lấy lịch sử conversation — `?? true` → `?? false` trong `getConversationHistory`, mặc định chỉ trả `message` và `notice` thay vì tất cả action types
- **aiwm/chat-gateway**: bỏ post-filter `type === 'message'` trong handler `conversation:history`, fix lỗi pagination (`total`/`hasMore` không khớp với số bản ghi thực trả về); `notice` action giờ được map thành `type: 'system'` đúng theo thiết kế
