# v2.13.0 — 2026-05-29

## Features
- **aiwm/heartbeat**: trả `instruction` trong response khi agent heartbeat với `status=idle` — agent tự động nhận instruction mới nhất mà không cần reconnect
- **aiwm/instruction-builder**: tách logic build instruction thành `InstructionBuilderService` dùng chung cho `AgentService` và `HeartbeatService`
