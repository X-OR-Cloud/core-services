# v1.13.0 — 2026-05-05

## Features
- **iam/app**: Thêm `allowOrigins` — whitelist origin frontend được phép SSO với App config
- **iam/app**: Thêm `webhookUrl` + `webhookSecret` — fire-and-forget webhook khi user mới được tạo qua SSO
- **iam/auth**: Validate `callbackUrl` origin trước khi redirect sang Google — trả `origin_not_allowed` nếu không trong whitelist
- **iam/auth**: Gọi webhook `user.created` sau khi tạo user mới qua Google SSO (kèm HMAC-SHA256 signature)
