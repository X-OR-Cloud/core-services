# v1.11.4 — 2026-05-01

## Fixes
- **cbm/provider**: cập nhật toàn bộ `@payos/node` v2 API — constructor dùng options object, `verifyPaymentWebhookData` → `webhooks.verify()`, `getPaymentLinkInformation` → `paymentRequests.get()`
- **cbm/payment**: `createPaymentLink` → `paymentRequests.create()` theo v2 API
- **cbm/provider**: wrap PayOS constructor trong try-catch, validate credentials trước khi init
