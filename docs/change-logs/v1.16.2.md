# v1.16.2 — 2026-05-08

## Fixes
- **iam/queue**: Fix Redis connection using wrong host (`localhost`) on production — switched `BullModule.forRoot` to `BullModule.forRootAsync` so Redis config is read after `ConfigModule` has loaded env vars, preventing `ECONNREFUSED 127.0.0.1:6379` that blocked user creation
