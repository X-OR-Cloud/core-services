# v1.19.0 — 2026-05-08

## Features
- **cbm/order**: Add `dateFrom`/`dateTo` query params to `GET /cbm/orders` for server-side date filtering
- **cbm/order**: Add compound index `{ 'owner.orgId', createdAt }` for efficient date-range queries
- **cbm/order**: Validate max 90-day range — returns 400 if exceeded
