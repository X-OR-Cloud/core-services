# v1.16.1 — 2026-05-08

## Fixes
- **aiwm/agent-gateway**: engineer agent miss `message:new` events khi conversation cũ + presence:agent:* Redis key không persist. Bỏ phụ thuộc vào `PresenceService.getAgentSocketIds` cho luồng chat — chuyển sang dùng per-agent Socket.IO room (`agent:${agentId}`) cho việc routing tin nhắn:
  - `handleConnection` kick stale sockets cùng agentId rồi `client.join('agent:${agentId}')` — đảm bảo single-socket-per-agent
  - `chat:message-new` handler dùng `this.server.in('agent:${agentId}').socketsJoin(...)` thay vì lookup presence
  - Auto-rejoin bỏ cutoff 24h — query mọi conversation `status: 'active'` để cover proactive emits (typing, system events)

## Notes
- **Không** fix root cause của `setAgentOnline` write Redis fail — chỉ né. Các path khác (vd `outbound:command`) vẫn dùng `presenceService.getAgentSocketIds` và có thể bị ảnh hưởng tương tự, sẽ xử lý sau.
- **Trade-off**: brief overlap khi agent reconnect không clean disconnect → 2 sockets cùng agentId tạm tồn tại; mitigation bằng cách kick stale sockets trong handleConnection.
