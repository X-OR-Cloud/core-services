# v1.12.2 — 2026-04-30

## Fixes
- **aiwm/connection-worker**: forward `notice` type messages to Discord/Telegram by default — trước đây chỉ `message` được forward, khiến response của `/inspect` (type=system → notice) bị bỏ qua và không hiện trong Discord
