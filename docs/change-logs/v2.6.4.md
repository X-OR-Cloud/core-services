# v2.6.4 — 2026-05-18

## Fixes
- **cbm/order**: BUG-001 — Dashboard stats dùng SUM(quantity) thay vì COUNT(order) cho từng món
  - Field `orderCount` đổi thành `totalQuantity` trong response `GET /orders/stats`
  - `totalAmount` không bị ảnh hưởng
