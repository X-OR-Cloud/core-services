# v1.19.2 — 2026-05-08

## Fixes
- **order**: Fix duplicate order code when all previous orders are soft-deleted. `generateCode()` now includes soft-deleted orders when calculating the sequence number, preventing unique index collision.
