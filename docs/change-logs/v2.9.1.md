# v2.9.1 — 2026-05-28

## Fixes
- **cbm/document**: Chặn `commitDraft` với content rỗng — trả về 400 nếu editor chưa load xong khi user nhấn Save, tránh mất toàn bộ nội dung document
- **cbm/document-rtc**: Thêm commit-lock (Redis TTL 10s) để ngăn race condition giữa `commitDraft` và `onStoreDocument` debounce của Hocuspocus — tránh draftState cũ ghi đè lên sau khi commit
- **cbm/document**: Sửa `updatedBy` trong `commitDraft` — trước đó gán cả `RequestContext` object thay vì `userId`/`agentId`
