# v1.17.0 — 2026-05-08

## Features
- **cbm/staff**: Thêm module Staff — proxy IAM endpoints (create/list/get/update/delete/reset-password) với guard self-disable/self-delete
- **cbm/outlet-member**: Thêm query `?userId=` vào `GET /cbm/outlet-members` — lấy outlet assignments của 1 user
- **cbm/outlet**: Update `validateOutletAccess()` — 403 response giờ có `code` field (`OUTLET_INACTIVE` / `OUTLET_NO_PERMISSION`)
