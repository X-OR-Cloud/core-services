# v2.9.0 — 2026-05-28

## Features

- **aiwm/agent**: Auto-inject project context vào instruction khi agent là `project.lead` — không cần `@project:<id>` thủ công. Format simplified gồm ID, summary, số member, số document.

## Fixes

- **aiwm/agent**: Phần document trong `@project:<id>` context giờ hiển thị tổng số tài liệu — `"X published trong tổng Y document"` thay vì chỉ đếm published.
