# v1.20.0 — 2026-05-08

## Features
- **order**: Add `GET /orders/stats` endpoint (F-005+F-006 Báo cáo Doanh thu). Returns `totalRevenue`, `totalOrders`, and item breakdown sorted by revenue. Restricted to `organization.owner`. Uses `$facet` aggregation with new compound index `{orgId, status, createdAt}`.
