# v2.8.11 — 2026-05-27

## Fixes
- **aiwm/vws**: add lại `toolConfig.functionCallingConfig.mode=AUTO` — native audio model cần config này để thực sự emit tool_call thay vì chỉ "thinking" rồi trả lời bằng audio. Root cause 1008 trước đây là sai format tool (đã fix ở v2.8.10), không phải do toolConfig.
