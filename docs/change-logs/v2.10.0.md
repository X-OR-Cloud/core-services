# v2.10.0 — 2026-05-29

## Features
- **aiwm/node**: thêm option `docker` vào setup guide — sinh script `start-xnode.sh` với setup token và URLs pre-filled; container tự bootstrap lần đầu, restart bằng `docker restart xnode` không cần chạy lại script
