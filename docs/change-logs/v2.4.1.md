# v2.4.1 — 2026-05-10

## Fixes

- **k8s/mb-bank**: Cập nhật tất cả YAML namespace từ `xai-prod` → `phuongn21-restricted` (namespace được cluster MB Bank cấp sẵn)
- **k8s/mb-bank**: Comment out `00-namespace.yaml` (namespace đã tồn tại, không cần tạo mới)
- **k8s/mb-bank/storage**: Thêm `pv-pvc-emptydir.yaml` — fallback PVC dùng StorageClass mặc định khi không có quyền tạo PV cluster-scoped
