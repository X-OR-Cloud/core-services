# v2.8.2 — 2026-05-27

## Features

- **aiwm/deployment**: `create()` tự động inject `metadata.modelType` và `metadata.modelProtocol` từ model vào deployment khi tạo mới
- **aiwm/db**: Migrate backfill `metadata.modelType` / `metadata.modelProtocol` cho 35 deployments hiện có trong `core_aiwm`
