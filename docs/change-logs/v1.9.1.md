# v1.9.1 — 2026-04-28

## Fixes

- **cbm/expense**: fix expense approve không tạo Transaction — nguyên nhân do spread Mongoose Document trực tiếp mà không gọi `.toObject()`, khiến `amount` và `date` bị mất (Mongoose non-enumerable getters). Fix: dùng `toObject()` trước khi spread.
- **vsm/nodes**: Thêm `PATCH /nodes/:id/status` endpoint (AmiAuthGuard) để AMI bridge cập nhật trạng thái kết nối
- **vsm/ami-client**: Sử dụng `http://localhost:3390` (direct port) cho VSM_API_URL thay vì nginx proxy — fix sync-result webhook không đến được API
- **vsm/phone-numbers**: Thêm `GET /phone-numbers/resolve/:number` endpoint
- **vsm/routes**: Thêm `POST /routes/resolve` endpoint
- **vsm/calls**: Thêm `GET /calls/stats` endpoint

## Notes

- `VSM_API_URL` trong `.env` đổi từ `https://api.hydrabyte.co/staging/vsm` → `http://localhost:3390`
