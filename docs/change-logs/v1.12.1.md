# v1.12.1 — 2026-04-30

## Fixes
- **aiwm/presence**: fix race condition in `setAgentOffline` — bỏ `SCARD + DEL` sau `SREM` để tránh xóa nhầm presence key của socket mới khi agent reconnect nhanh, khiến `/inspect` và `getAgentSocketIds` luôn trả về `[]`
