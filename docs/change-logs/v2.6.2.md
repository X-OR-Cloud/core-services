# v2.6.2 — 2026-05-16

## Fixes
- **cbm/order**: Fix `statistics.byStatus` — giờ apply đúng dateFrom/dateTo filter thay vì tính all-time
- **cbm/order**: Fix search bị ignore khi có date filter (đặt `$or` vào trong `filter{}` thay vì top-level)

## Features
- **cbm/order**: Search theo tên món (`items.name`) — case-insensitive, kết hợp với search code/customer name/phone
